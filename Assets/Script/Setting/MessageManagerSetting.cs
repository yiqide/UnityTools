
public enum ESendType
{
    /// <summary>
    /// 作为发送者是无效的
    /// </summary>
    EAll,
    Default
}

public enum EReceiveType
{
    /// <summary>
    /// 作为注册者时无效的
    /// </summary>
    EAll,
    Default
}
